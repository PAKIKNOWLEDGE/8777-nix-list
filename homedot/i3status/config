# i3status — Nerd Font 图标版
general {
    colors = true
    interval = 5
}

order += "wireless _first_"
order += "ethernet _first_"
order += "volume master"
order += "cpu_usage"
order += "memory"
order += "cpu_temperature 0"
order += "battery all"
order += "tztime local"

wireless _first_ {
    format_up = "   %quality"
    format_down = "   离线"
}

ethernet _first_ {
    format_up = "   %ip"
    format_down = ""
}

volume master {
    format = "   %volume"
    format_muted = "   "
    device = "pulse"
    mixer = "Master"
}

cpu_usage {
    format = "   %usage"
}

memory {
    format = "   %used"
}

cpu_temperature 0 {
    format = "   %degrees°C"
}

 battery all {
     format = " 󰁹  %percentage"
     last_full_capacity = true
 }



tztime local {
    format = "   %m-%d %H:%M"
}
